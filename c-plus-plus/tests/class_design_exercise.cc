#include <iostream>
#include <ostream>
#include <string>

using std::cout;

class my_class {
private:
    std::string name;
    std::string sex;
    bool mir;
    public:

    my_class& operator << (std::ostream& os, )
}

    int main(int argc, char *argv[])
    {

        return 0;
    }
